<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Pantheon weekly sync LaunchAgent.
  Install: copy to ~/Library/LaunchAgents/com.petpooja.pantheon-sync.plist
  then `launchctl load -w ~/Library/LaunchAgents/com.petpooja.pantheon-sync.plist`.

  Replace YOUR_USERNAME below with `whoami` output (or use the install script,
  which does the substitution automatically).

  Schedule: every Monday at 09:00 local time. If the Mac is asleep at 09:00,
  launchd runs the task as soon as it wakes (StartCalendarInterval semantics).
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.petpooja.pantheon-sync</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/YOUR_USERNAME/.claude/skills/pantheon-design-system/scripts/scheduled-sync.sh</string>
    </array>

    <key>StartCalendarInterval</key>
    <dict>
        <key>Weekday</key>
        <integer>1</integer>
        <key>Hour</key>
        <integer>9</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <key>RunAtLoad</key>
    <false/>

    <key>StandardOutPath</key>
    <string>/Users/YOUR_USERNAME/.claude/logs/pantheon-sync.stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/YOUR_USERNAME/.claude/logs/pantheon-sync.stderr.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>HOME</key>
        <string>/Users/YOUR_USERNAME</string>
        <key>PATH</key>
        <string>/Users/YOUR_USERNAME/.local/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    </dict>
</dict>
</plist>
