# Pantheon — Reference Index

This file has been superseded. Content is split across three focused references:

- **`tokens.md`** — every color token (semantic + primitive ramps), full Prometheus type scale, sizing, radius, elevation. 387 lines.
- **`components.md`** — every component, variant axes, states, sizes, composition tips, gotchas. 630 lines.
- **`full_inventory.md`** — raw 1,785-line per-page file inventory with node IDs, variant counts, dimensions. Load only when you need specific node IDs.
- **`raw_variables_extraction.md`** — the raw `get_variable_defs` dump across all 23 pages with per-page counts and collision notes.

If unsure which file to read:

| Question | File |
|---|---|
| "What's the hex for `Surface/Brand`?" | `tokens.md` |
| "How many variants does Button have?" | `components.md` |
| "What's the node ID of the Tooltip page?" | `full_inventory.md` |
| "What typography token is the default body style?" | `tokens.md` |
| "Does Pantheon have a Date Picker?" | `components.md` (no — page exists but is empty) |
| "What's the corner radius scale?" | `tokens.md` |
